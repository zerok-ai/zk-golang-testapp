apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: loadrun-error
  name: loadrun-error-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loadrun-error
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: loadrun-error
    spec:
      containers:
        - args:
            - |-
              while true; do sleep 20; grpcurl -plaintext -d '{"code": 3, "forward": "zk-calc-app-02"}' -H 'traceparent: 00-dddddddd'$(tr -dc 'a-f0-9' < /dev/urandom | head -c24)'-'$(tr -dc 'a-f0-9' < /dev/urandom | head -c16)'-01' zk-calc-app-01.zk-calc-app.svc.cluster.local:80 calculator.Calculator.Error; done;
          command:
            - sh
            - -c
            - --
          image: chibk/grpcurl
          imagePullPolicy: Always
          name: curl
          resources:
            limits:
              cpu: "2"
              memory: 1Gi
            requests:
              cpu: 10m
              memory: 40Mi
